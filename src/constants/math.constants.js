/**
 * @fileOverview 定义了项目中使用的通用数学常量。
 */

/**
 * @description 一个极小值，用于浮点数比较时的容差。
 */
export const EPS = 0.000001;

/**
 * @description 圆周率 Pi。
 */
export const PI = Math.PI;

/**
 * @description 2 * Pi。
 */
export const TWO_PI = Math.PI * 2;

/**
 * @description Pi / 2。
 */
export const HALF_PI = Math.PI / 2;
